@* @model IEnumerable<List>

<h2>Liste de cumpărături</h2>

<style>
    .shopping-list {
        border: 1px solid black;
        border-radius: 5px;
        padding: 10px;
        margin-bottom: 20px;
    }

</style>
<form asp-action="AddList" method="post">
    <input type="text" name="name" placeholder="Title" />
    <button type="submit">Adaugă listă</button>
</form>

@foreach (var list in Model)
{
 <div class="shopping-list"> <!-- Adaugă clasa CSS aici -->
    <h3>@list.Name</h3>
    <form asp-action="AddItem" method="post">
        <input type="hidden" name="listId" value="@list.Id" />
        <input type="text" name="text" placeholder="item" />
        <button type="submit">Adaugă</button>
    </form>

    <form asp-action="DeleteList" method="post" style="display:inline;">
        <input type="hidden" name="id" value="@list.Id" />
        <button type="submit">Șterge lista</button>
    </form>

    <ul>
        @foreach (var item in list.Items)
        {
            <li>
                <form asp-action="ToggleItem" method="post" style="display:inline;">
                    <input type="hidden" name="id" value="@item.Id" />
                    <input type="checkbox" onchange="this.form.submit();" @(item.IsChecked ? "checked" : "") />
                    @Html.DisplayFor(modelItem => item.Text)
                </form>

                <form asp-action="DeleteItem" method="post" style="display:inline;">
                    <input type="hidden" name="id" value="@item.Id" />
                        <button type="submit">🗑</button>
                </form>

            </li>
        }
    </ul>
  </div>
}
 *@


 @model IEnumerable<List>

<style>
    body {
        background-image: url('https://i.imgur.com/FmeFUGh.jpg');
        background-size: cover;
        background-position: center;
        background-attachment: fixed;
    }

    .recipe-container {
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
        justify-content: center;
    }

    .titlu {
       /*  font-family: 'Dancing Script', cursive; */
        text-align: center;
        font-size: 60px;
        color: #003300;
        width: 100%;
    }

    .buton {
        border-radius: 10px;
        padding: 10px;
/*         background-color: antiquewhite;
        border-color: antiquewhite; */
        box-sizing: border-box;
        background-color: rgba(255, 255, 255, 0.3);
        border: 3px dotted black;
        color: black;
        display: flex;
        align-items: center;
        justify-content: center;
        box-sizing: border-box;
        cursor: pointer;
        width: calc(25% - 40px); /* Fiecare rând va avea 4 elemente */
        text-align: center;
    }

    .butondel {
        background: none;
        border-radius: 10px;
        border: none;
        cursor: pointer;
        padding: 5px;
        position: absolute;
        top: 10px;
        right: 10px;
    }

    .shopping-list {
        position: relative;
        border: 1px solid black;
        border-radius: 10px;
        padding: 10px;
        width: calc(25% - 40px); /* Fiecare rând va avea 4 elemente */
        height: auto; /* Permite div-urilor să fie mai înalte */
        box-sizing: border-box;
        background-color: rgba(255, 255, 255, 0.5);
        border: 1px solid rgba(0, 0, 0, 0.2);
        color: black;
    }

        .shopping-list h3 {
            margin-top: 0;
        }

        .shopping-list form {
            margin-bottom: 10px;
        }

        .shopping-list ul {
            padding-left: 20px;
        }

            .shopping-list ul li {
                display: flex;
                align-items: center;
                justify-content: space-between;
                margin-bottom: 5px;
            }

    .deleteItem {
        background: none;
        border: none;
        cursor: pointer;
        padding: 0;
        margin: 0;
    }

        .deleteItem:hover {
            color: red;
        }

    .additem {
        border: none;
        background: none;
        outline: none; /* Asigură că nu apare conturul implicit la focus */
        color: black;
        padding-left: 20px;
        padding-top: 2px;
    }

    .custom-checkbox {
        width: 16px;
        height: 16px;
        border: 1px solid;
        border-radius: 3px;
    }
    
    .addList{
        border: black;
        color: red;
    }

</style>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        document.querySelectorAll('.item-input').forEach(function (input) {
            input.addEventListener('keypress', function (event) {
                if (event.key === 'Enter') {
                    event.preventDefault();
                    this.form.submit();
                }
            });
        });
    });
</script>

<div class="titlu">Shopping Lists</></div>
<br /><br />
<div class="recipe-container">
    <!-- Div pentru butonul "Add List" -->
    <div class="buton">
        <form asp-action="AddList" method="post">
            <input type="text" name="name" placeholder="+ New List Title" style="border: none; background: none; outline: none; width: 80%; text-align: center;" />
            <button class="addList" type="submit" style="border: none; background: none; outline: none; cursor: pointer;">Add List</button>
        </form>
    </div>

    <!-- Divuri pentru listele de cumpărături -->
    @foreach (var list in Model)
    {
        <div class="shopping-list">
            <button class="butondel" type="submit" form="delete-list-form-@list.Id">❌</button>
            <br />
            <h3>@list.Name</h3>

            <form id="delete-list-form-@list.Id" asp-action="DeleteList" method="post" style="display:none;">
                <input type="hidden" name="id" value="@list.Id" />
            </form>

            <ul>
                @foreach (var item in list.Items)
                {
                    <li>
                        <form asp-action="ToggleItem" method="post" style="display:inline;">
                            <input type="hidden" name="id" value="@item.Id" />
                            <input class="custom-checkbox" type="checkbox" onchange="this.form.submit();" @(item.IsChecked ? "checked" : "") />
                            @Html.DisplayFor(modelItem => item.Text)
                        </form>

                        <form asp-action="DeleteItem" method="post" style="display:inline;">
                            <input type="hidden" name="id" value="@item.Id" />
                            <button class="deleteItem" type="submit">🗑</button>
                        </form>
                    </li>
                }
            </ul>

            <form asp-action="AddItem" method="post">
                <input type="hidden" name="listId" value="@list.Id" />
                <input class="additem" type="text" name="text" placeholder="+ List Item" class="item-input" />
            </form>
        </div>
    }
</div>